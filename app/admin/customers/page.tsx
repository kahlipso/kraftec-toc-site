import Link from 'next/link';
import { getAdminCustomers } from '@/app/lib/admin';

export default async function CustomersPage() { const customers = await getAdminCustomers(); return <><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-widest text-[#d01111]">Customer history</p><h2 className="mt-2 text-4xl font-bold tracking-tight">Customers</h2></div><Link href="/admin/customers/new" className="shrink-0 rounded-full bg-[#d01111] px-5 py-2.5 text-sm font-semibold text-white">New customer</Link></div><div className="mt-8 overflow-hidden rounded-2xl border border-gray-200 bg-white"><table className="w-full text-left text-sm"><thead className="bg-[#fafafa] text-xs uppercase tracking-wide text-zinc-500"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Phone</th><th className="px-5 py-4">Requests</th><th className="px-5 py-4">Latest request</th><th className="px-5 py-4" /></tr></thead><tbody>{customers.map((customer) => <tr key={customer.id} className="border-t border-gray-100"><td className="px-5 py-4 font-semibold">{customer.name}</td><td className="px-5 py-4 text-zinc-600">{customer.phone}</td><td className="px-5 py-4">{customer.requestCount}</td><td className="px-5 py-4 text-zinc-600">{customer.latestRequest ? new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', year: 'numeric' }).format(new Date(customer.latestRequest)) : '—'}</td><td className="px-5 py-4 text-right"><Link href={`/admin/customers/${customer.id}`} className="font-semibold text-[#d01111] hover:underline">Manage</Link></td></tr>)}</tbody></table></div></> }
