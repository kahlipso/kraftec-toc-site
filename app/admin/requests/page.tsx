import Link from 'next/link';
import { getAdminBookings } from '@/app/lib/admin';

export default async function RequestsPage() {
  const requests = await getAdminBookings();
  return <><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-widest text-[#d01111]">Customer operations</p><h2 className="mt-2 text-4xl font-bold tracking-tight">Requests</h2></div><Link href="/admin/requests/new" className="shrink-0 rounded-full bg-[#d01111] px-5 py-2.5 text-sm font-semibold text-white">New request</Link></div><div className="mt-8 overflow-hidden rounded-2xl border border-gray-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-[#fafafa] text-xs uppercase tracking-wide text-zinc-500"><tr><th className="px-5 py-4">Customer</th><th className="px-5 py-4">Technician</th><th className="px-5 py-4">Appointment</th><th className="px-5 py-4">Status</th><th className="px-5 py-4" /></tr></thead><tbody>{requests.map((request) => <tr key={request.id} className="border-t border-gray-100"><td className="px-5 py-4"><p className="font-semibold">{request.customerName}</p><p className="text-xs text-zinc-500">{request.address}</p></td><td className="px-5 py-4">{request.proName}</td><td className="px-5 py-4">{new Intl.DateTimeFormat('en-US', { month: 'short', day: 'numeric', hour: 'numeric', minute: '2-digit' }).format(new Date(request.slotStart))}</td><td className="px-5 py-4"><span className="rounded-full bg-zinc-100 px-2.5 py-1 text-xs font-medium capitalize">{request.status.replace('_', ' ')}</span></td><td className="px-5 py-4 text-right"><Link className="font-semibold text-[#d01111] hover:underline" href={`/admin/requests/${request.id}`}>Manage</Link></td></tr>)}</tbody></table></div>{!requests.length && <p className="p-10 text-center text-sm text-zinc-500">No active requests.</p>}</div></>;
}
